% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_plausibility}
\alias{audit_plausibility}
\title{Audit soft plausibility of generated lives across dimensions}
\usage{
audit_plausibility(
  N = 5000L,
  by = "gender",
  lang = "no",
  dims = c("hobbies", "diet", "party", "religion", "orientation", "education",
    "occupation", "marital_status", "disability", "close_friends"),
  min_n = 15L,
  top = 12L,
  verbose = TRUE
)
}
\arguments{
\item{N}{Number of lives. Default 5000.}

\item{by}{Grouping variable, a top-level field. Default \code{"gender"}.}

\item{lang}{Language. Default \code{"no"}.}

\item{dims}{Dimensions to audit. Default covers the main categorical ones.}

\item{min_n}{Minimum occurrences of a value before it is reported. Default 15.}

\item{top}{Show only the \code{top} most- and least-skewed values per
dimension (0 = all). Default 12.}

\item{verbose}{Print a report. Default TRUE.}
}
\value{
Invisibly: a named list of data frames (one per dimension).
}
\description{
Generates N lives and, for each categorical dimension, reports how each
value splits across a grouping variable (default \code{"gender"}). The
point is to surface combinations that are possible but appear far more
often than is believable — the kind of thing that drains realism even
though it is not strictly impossible.
}
