% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constrained.R
\name{counterfact_me_constrained}
\alias{counterfact_me_constrained}
\title{Generate a counterfactual life with constraints}
\usage{
counterfact_me_constrained(givens = list(),
                                       max_attempts = 500L,
                                       lang = c("no", "en"),
                                       dimensions = available_dimensions(),
                                       verbose = FALSE)
}
\arguments{
\item{givens}{A named list of constraints, e.g.,
  \code{list(age = 42, gender = "M", county = "Oslo")}.
  Supported fields: \code{age}, \code{gender}, \code{county},
  \code{municipality}, \code{occupation}, \code{education},
  \code{marital_status}, \code{religion}, \code{party}, \code{background}.}

\item{max_attempts}{Max rejection-sampling attempts. Default 500.}

\item{lang}{Language for labels. Default "no".}

\item{dimensions}{Which dimensions to draw. Default: all.}

\item{verbose}{If TRUE, print attempts info.}
}
\value{
A \code{counterfactme} object with the constraints respected.
}
\description{
Like \code{\link{counterfact_me}} but lets you fix some dimensions to
specific values while letting the rest be drawn conditionally. Uses
rejection sampling with up to \code{max_attempts} tries; if no match is
found, falls back to direct override (with a warning).
}
\examples{
\dontrun{
counterfact_me_constrained(list(age = 42, gender = "M", county = "Oslo"))
counterfact_me_constrained(list(
  age = 30, gender = "F", occupation_substring = "lærer"
))
}
}
